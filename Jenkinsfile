pipeline {

		agent {
                         label {
                                    label 'built-in'
                                    customWorkspace '/mnt/myproject'
                         }
                }
		
		stages {
		
			stage ('Stage-1') {
			
				steps {
				
				    sh "mkdir test"
				
				}
			
			}
			
			stage ('Stage-2') {
			
				steps {
				
				    sh "mkdir folder"
				
				}
			
			}
                }
}
			
